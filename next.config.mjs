/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
                protocol:'https',
                hostname:"govwklbidoldosqqvriv.supabase.co"
            }
        ]
    }
};

export default nextConfig;
